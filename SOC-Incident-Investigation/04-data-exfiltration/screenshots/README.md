# Screenshots — Incident 04 (Data exfiltration)

**Purpose:** Capture large outbound transfer views, archive staging, and SOAR results for destination IP/domain.

**Tools:** SIEM, spreadsheet on CSV output, `awk`/`grep` terminal, SOAR UI.

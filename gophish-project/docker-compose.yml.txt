version: '3.8'

services:
  gophish:
    image: gophish/gophish:latest
    container_name: gophish
    ports:
      - "3333:3333"   # Admin dashboard
      - "8080:80"     # Phishing listener (landing pages)
    volumes:
      - ./gophish_data:/app/data
    restart: unless-stopped